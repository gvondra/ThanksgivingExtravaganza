import { InvitationResponse } from './invitation-response';

describe('InvitationResponse', () => {
  it('should create an instance', () => {
    expect(new InvitationResponse()).toBeTruthy();
  });
});
